import 'package:flutter/widgets.dart';

class AppSpacing {
  AppSpacing._();

  static const EdgeInsets screen = EdgeInsets.all(24);

  static const EdgeInsets card = EdgeInsets.all(16);

  static const EdgeInsets horizontal = EdgeInsets.symmetric(horizontal: 24);

  static const EdgeInsets vertical = EdgeInsets.symmetric(vertical: 24);

  static const EdgeInsets input = EdgeInsets.symmetric(
    horizontal: 16,
    vertical: 14,
  );

  static const EdgeInsets button = EdgeInsets.symmetric(
    horizontal: 20,
    vertical: 14,
  );
}
